# Wedding Invite

A standalone interactive wedding invitation featuring a sealed envelope,
floral liner, wax seal, and full-screen invitation reveal.

Open `index.html` directly in a browser or publish this folder as-is. The
page is self-contained, so it does not depend on the separate `heirloom`
project.
